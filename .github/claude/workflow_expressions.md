### 표현 정리 워크플로우
- 사용자가 말한 전체 스크립트는 `.github/answers/` 폴더 내의 md 파일에 있다.
- 사용자가 복습하고자 하는 내용을 알려주면, 짧은 설명과 함께 `.github/expressions/` 폴더에 별도의 파일을 생성한다.
- 파일명은 날짜 없이 한국어로 띄어쓰기 없이 간단한 설명만 쓴다. (예: `피드백요청시.md`)
- 레포지토리 루트의 `README.md` 맨 아래에 해당 내용을 추가한다.